DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'models_db',   # Change this to your database name
        'USER': 'thejoker',
        'PASSWORD': '',
        'HOST': 'localhost',   # Change this to your database host if it's not localhost
        'PORT': '3306',        # Change this to your database port if needed
    }
}
